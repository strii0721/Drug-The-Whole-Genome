# =============================================
# DrugCLIP (Drug-The-Whole-Genome) — arm64 / DGX Spark requirements
# =============================================
# NOTE: torch / torchvision / torchaudio 由 Dockerfile 从 cu130 索引安装
# (torch==2.9.1+cu130；pip 轮子自带 CUDA 库，host driver >= 580 即可)，
# 此处不要重装，避免覆盖 cu130 构建。
#
# arm64 说明（详见 _repro_report_arm64_compat.md）：
#   - numpy==1.26.4 必须 pin：pandas 1.5.3 / scipy 1.10.1 与 numpy 2.x 不兼容
#   - biopython>=1.84：1.81 无 linux_aarch64 轮子，升 1.85
#   - h5py>=3.12.1：3.11.0 及以下无 linux_aarch64 轮子
#   - rdkit / pandas / scikit-learn / scipy 均为 py311 aarch64 有轮子的版本，勿升级

# =============================================
# Common Used Tools
# =============================================
numpy==1.26.4
pandas==1.5.3
scikit-learn==1.2.2
scipy==1.10.1

# =============================================
# Protein & Molecule
# =============================================
rdkit==2023.9.5
biopython==1.85
h5py>=3.12.1

# =============================================
# Utils
# =============================================
python-dotenv   # loading env variables from .env file
rich            # beautiful text formatting in terminal
pytest          # tests
ipython         # convenient interactive shell
jupyterlab      # JupyterLab to run Jupyter Notebooks
jsonlines       # for reading and writing jsonl files

# =============================================
# Linters
# =============================================
pre-commit      # hooks for applying linters on commit
black           # code formatting
isort           # import sorting
flake8          # code analysis
nbstripout      # remove output from jupyter notebooks
